---
const trustSignals = [
	"25+ års erfaring",
	"Dansk og engelsk",
	"Hjemme, online og Østerbro",
];
---

<html lang="da">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta
			name="description"
			content="Samtaler og familievejledning med Maria Baad på Østerbro, hjemme hos jer eller online. Støtte til familier og praksisnære forløb for institutioner."
		/>
		<meta name="generator" content={Astro.generator} />
		<title>Maria Baad | Familievejledning</title>
	</head>
	<body>
		<a class="skip-link" href="#indhold">Spring til indhold</a>
		<header class="site-header">
			<a class="brand" href="/" aria-label="Maria Baad forside">
				<span class="brand-mark">MB</span>
				<span>
					<strong>Maria Baad</strong>
					<small>Familievejledning</small>
				</span>
			</a>
			<nav class="main-nav" aria-label="Primær navigation">
				<a href="#familier">Familier</a>
				<a href="#anbefaling">Anbefaling</a>
				<a href="#om">Om mig</a>
				<a href="#institutioner">Institutioner</a>
				<a href="#kontakt">Kontakt</a>
			</nav>
		</header>

		<main id="indhold">
			<section class="hero" aria-labelledby="hero-title">
				<div class="hero-copy">
					<p class="eyebrow">Familievejledning på Østerbro og online</p>
					<h1 id="hero-title">Samtaler og familievejledning</h1>
					<p class="hero-lede">
						Jeg hjælper forældre og familier, når noget føles svært, fastlåst eller
						uoverskueligt.
					</p>
					<div class="hero-actions" aria-label="Vigtige handlinger">
						<a class="button primary" href="#kontakt">Kontakt mig</a>
						<a class="button secondary" href="#familier">Læs mere</a>
					</div>
					<ul class="trust-list" aria-label="Nøgletal">
						{trustSignals.map((signal) => <li>{signal}</li>)}
					</ul>
				</div>
				<div class="hero-media" aria-label="Kort om mig">
					<div class="portrait-panel">
						<img
							src="/images/maria-baad-hero.webp"
							alt="Maria Baad i et lyst og roligt portrætfoto"
							width="1000"
							height="667"
						/>
					</div>
				</div>
			</section>

			<section class="section prose-section" id="familier" aria-labelledby="familier-title">
				<div class="section-heading">
					<p class="eyebrow">For familier</p>
					<h2 id="familier-title">Samtaler og familievejledning</h2>
				</div>
				<div class="prose-flow">
					<p>Jeg hjælper forældre og familier, når noget føles svært, fastlåst eller uoverskueligt.</p>
					<p>
						Sammen forsøger vi at skabe større forståelse for det, der ligger bag barnets reaktioner,
						familiens mønstre og de situationer, der kalder på støtte, retning eller nye perspektiver.
					</p>
					<p>
						Jeg møder familier med varme, ærlighed og nysgerrighed — uanset om det handler om små eller
						store børn, store følelser, konflikter, mistrivsel, tvivl i forældreskabet eller samarbejde
						omkring barnet.
					</p>
					<p>Samtalerne kan foregå hjemme hos jer, online eller fra mit kontor på Østerbro i København.</p>
					<p>
						Jeg tilbyder vejledning på både dansk og engelsk og arbejder med både danske og internationale
						familier.
					</p>
				</div>
			</section>

			<section class="quote-band" id="anbefaling" aria-labelledby="quote-title">
				<div>
					<p class="eyebrow">Anbefaling</p>
					<h2 id="quote-title">
						<a class="quote-title-link" href="#anbefaling">Anbefaling</a>
					</h2>
				</div>
			</section>

			<section class="section prose-section" id="om" aria-labelledby="about-title">
				<div class="section-heading">
					<p class="eyebrow">Om mig</p>
					<h2 id="about-title">Jeg hedder Maria Baad Andersen</h2>
				</div>
				<div class="prose-flow">
					<p>
						Jeg hedder Maria Baad Andersen og arbejder som familievejleder med børn, forældre og familier.
					</p>
					<p>
						Jeg hjælper familier med at skabe større forståelse for det, der bliver svært — hvad enten det
						handler om trivsel, relationer, følelser eller usikkerhed i forældreskabet.
					</p>
					<p>
						Det er vigtigt for mig at skabe et trygt rum, hvor mennesker føler sig mødt, forstået og mindre
						alene i det, der er svært.
					</p>
					<p>
						Jeg har mere end 25 års erfaring med arbejdet omkring børn og familier og arbejder blandt andet
						med tilknytning, mentalisering og Tryghedscirklen.
					</p>
					<p>
						Du kan læse mere om min faglige baggrund og erfaring på
						<a
							class="inline-link"
							href="https://www.linkedin.com/in/maria-baad-andersen-6b454332a/"
							target="_blank"
							rel="noreferrer"
						>
							LinkedIn
						</a>
						.
					</p>
				</div>
			</section>

			<section class="section prose-section institution-section" id="institutioner" aria-labelledby="institution-title">
				<div class="section-heading">
					<p class="eyebrow">For institutioner</p>
					<h2 id="institution-title">Kompetenceudvikling og praksisnære forløb</h2>
				</div>
				<div class="prose-flow">
					<p>
						Jeg tilbyder kompetenceudvikling, supervision og praksisnære forløb for daginstitutioner, skoler
						og kommuner — med afsæt i Tryghedscirklen og Se Barnet Indefra.
					</p>
					<p>
						Med et nysgerrigt og respektfuldt udgangspunkt hjælper jeg personalegrupper med at forstå barnets
						behov bag adfærden og skabe fælles refleksion, tydelighed og styrket trivsel i hverdagen.
					</p>
					<p>
						Jeg har mange års erfaring med arbejdet omkring børn i udsatte positioner, tilknytning,
						mentalisering og samarbejdet mellem børn, forældre og fagpersoner.
					</p>
					<p>
						Jeg tilbyder blandt andet workshops, supervision, faciliterede forældreaftener og længerevarende
						forløb tilpasset den enkelte institutions behov.
					</p>
					<p>Kontakt mig gerne for en uforpligtende samtale eller et konkret tilbud.</p>
				</div>
			</section>

			<section class="contact-section" id="kontakt" aria-labelledby="contact-title">
				<div>
					<p class="eyebrow">Kontakt</p>
					<h2 id="contact-title">Kontakt mig gerne</h2>
					<p>Kontakt mig gerne for en uforpligtende samtale eller et konkret tilbud.</p>
				</div>
				<div class="contact-actions">
					<a class="button primary" href="tel:+4593901154">Ring til mig</a>
					<a class="button secondary" href="mailto:kontaktmariabaad@gmail.com">Skriv e-mail</a>
				</div>
			</section>
		</main>

		<footer class="site-footer">
			<p>Maria Baad</p>
			<p>Telefon: <a href="tel:+4593901154">+45 93901154</a></p>
			<p>CVR: 45155099</p>
		</footer>
	</body>
</html>

<style>
	:root {
		color-scheme: light;
		--paper: #faf6ef;
		--surface: #fffdf8;
		--ink: #1f2d2a;
		--muted: #5f6f69;
		--sage: #dce8df;
		--sage-soft: #edf4ee;
		--sage-dark: #31564e;
		--clay: #a95d4c;
		--clay-soft: #f2ddd5;
		--butter: #f2d88f;
		--sky: #d9e8ec;
		--rose: #ead7d3;
		--white: #fffdf8;
		--line: rgba(34, 48, 43, 0.14);
		--shadow: 0 22px 55px rgba(34, 48, 43, 0.1);
		--shadow-soft: 0 14px 32px rgba(34, 48, 43, 0.08);
		font-family:
			Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
	}

	* {
		box-sizing: border-box;
	}

	html {
		scroll-behavior: smooth;
		scroll-padding-top: 96px;
	}

	body {
		margin: 0;
		background: var(--paper);
		color: var(--ink);
		font-size: 16px;
		line-height: 1.6;
	}

	a {
		color: inherit;
	}

	:focus-visible {
		outline: 3px solid var(--clay);
		outline-offset: 4px;
	}

	.skip-link {
		position: fixed;
		top: 12px;
		left: 12px;
		z-index: 100;
		transform: translateY(-140%);
		padding: 10px 14px;
		background: var(--ink);
		border-radius: 7px;
		color: var(--white);
		font-weight: 800;
		text-decoration: none;
		transition: transform 180ms ease;
	}

	.skip-link:focus-visible {
		transform: translateY(0);
	}

	.site-header {
		position: sticky;
		top: 0;
		z-index: 20;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 24px;
		padding: 18px clamp(20px, 5vw, 72px);
		background:
			linear-gradient(180deg, rgba(237, 244, 238, 0.94), rgba(255, 253, 248, 0.9)),
			rgba(255, 253, 248, 0.9);
		border-bottom: 1px solid rgba(34, 48, 43, 0.18);
		box-shadow: 0 8px 22px rgba(34, 48, 43, 0.045);
		backdrop-filter: blur(18px);
		-webkit-backdrop-filter: blur(18px);
	}

	.brand {
		display: inline-flex;
		align-items: center;
		gap: 12px;
		text-decoration: none;
	}

	.brand-mark {
		display: grid;
		width: 44px;
		height: 44px;
		place-items: center;
		border-radius: 50%;
		background: linear-gradient(145deg, var(--sage-dark), #243e39);
		color: var(--white);
		font-weight: 700;
		box-shadow: var(--shadow-soft);
	}

	.brand strong,
	.brand small {
		display: block;
	}

	.brand small,
	.eyebrow {
		color: var(--muted);
		font-size: 0.84rem;
		font-weight: 700;
		text-transform: uppercase;
	}

	.main-nav {
		display: flex;
		flex-wrap: wrap;
		justify-content: flex-end;
		gap: 8px 22px;
		font-size: 0.95rem;
	}

	.main-nav a {
		min-height: 44px;
		display: inline-flex;
		align-items: center;
		text-decoration: none;
		text-underline-offset: 5px;
	}

	.main-nav a:hover,
	.main-nav a:focus-visible {
		color: var(--clay);
	}

	main {
		overflow: hidden;
	}

	.hero,
	.section,
	.quote-band,
	.contact-section,
	.site-footer {
		padding-inline: clamp(20px, 5vw, 72px);
	}

	.hero {
		display: grid;
		grid-template-columns: minmax(0, 1.04fr) minmax(320px, 0.78fr);
		gap: clamp(36px, 7vw, 92px);
		align-items: center;
		min-height: min(780px, calc(100dvh - 82px));
		padding-block: clamp(58px, 8vw, 92px) clamp(44px, 6vw, 72px);
		background:
			linear-gradient(180deg, rgba(220, 232, 223, 0.62), rgba(250, 246, 239, 0) 34%),
			linear-gradient(115deg, rgba(220, 232, 223, 0.78), transparent 50%),
			linear-gradient(22deg, rgba(217, 232, 236, 0.56), transparent 44%),
			var(--paper);
	}

	.eyebrow {
		margin: 0 0 14px;
		letter-spacing: 0;
	}

	h1,
	h2,
	h3,
	p {
		margin-top: 0;
	}

	h1,
	h2,
	h3 {
		line-height: 1.08;
		letter-spacing: 0;
	}

	h1 {
		max-width: 880px;
		margin-bottom: 22px;
		font-family: Georgia, "Times New Roman", serif;
		font-size: clamp(2.8rem, 6.7vw, 5.85rem);
		font-weight: 500;
	}

	h2 {
		max-width: 780px;
		margin-bottom: 20px;
		font-family: Georgia, "Times New Roman", serif;
		font-size: clamp(2.15rem, 4.6vw, 4.6rem);
		font-weight: 500;
	}

	h3 {
		margin-bottom: 10px;
		font-size: 1.2rem;
	}

	.hero-lede,
	.section p,
	.contact-section p {
		max-width: 680px;
		color: var(--muted);
		font-size: 1.08rem;
	}

	.hero-actions,
	.contact-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
		margin-top: 30px;
	}

	.button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 10px;
		min-height: 48px;
		padding: 13px 20px;
		border: 1px solid transparent;
		border-radius: 7px;
		font-weight: 800;
		text-decoration: none;
		box-shadow: 0 0 0 rgba(34, 48, 43, 0);
		transition:
			transform 180ms ease,
			box-shadow 180ms ease,
			background-color 180ms ease,
			border-color 180ms ease;
		touch-action: manipulation;
	}

	.button:hover {
		transform: translateY(-2px);
		box-shadow: var(--shadow-soft);
	}

	.button.primary {
		background: var(--sage-dark);
		color: var(--white);
	}

	.button.secondary {
		background: var(--white);
		border-color: var(--line);
		color: var(--sage-dark);
	}

	.button.primary::after,
	.button.secondary::after {
		content: "";
		width: 0.58em;
		height: 0.58em;
		border-top: 2px solid currentColor;
		border-right: 2px solid currentColor;
		transform: rotate(45deg);
	}

	.trust-list {
		display: flex;
		flex-wrap: wrap;
		gap: 10px;
		margin: 28px 0 0;
		padding: 0;
		list-style: none;
	}

	.trust-list li {
		padding: 9px 12px;
		background: rgba(255, 253, 248, 0.72);
		border: 1px solid var(--line);
		border-radius: 999px;
		color: var(--sage-dark);
		font-size: 0.9rem;
		font-weight: 800;
	}

	.hero-media,
	.about-media {
		min-width: 0;
	}

	.portrait-panel {
		position: relative;
		display: block;
		min-height: 560px;
		overflow: hidden;
		aspect-ratio: 4 / 5;
		background: var(--sage);
		border: 1px solid var(--line);
		border-radius: 8px;
		box-shadow: var(--shadow);
	}

	.portrait-panel::before {
		position: absolute;
		inset: 0;
		z-index: 1;
		content: "";
		background:
			linear-gradient(180deg, rgba(31, 45, 42, 0.02) 45%, rgba(31, 45, 42, 0.34)),
			linear-gradient(120deg, rgba(255, 253, 248, 0.16), rgba(255, 253, 248, 0));
		pointer-events: none;
	}

	.portrait-panel img {
		display: block;
		width: 100%;
		height: 100%;
		min-height: inherit;
		object-fit: cover;
		object-position: center;
	}

	.section {
		padding-block: clamp(70px, 10vw, 132px);
	}

	.prose-section {
		display: grid;
		grid-template-columns: minmax(0, 52rem);
		justify-content: center;
		gap: 24px;
		align-items: start;
	}

	.section-heading {
		max-width: 46rem;
	}

	.prose-flow {
		max-width: 46rem;
	}

	.prose-flow p + p {
		margin-top: 18px;
	}

	.inline-link {
		color: var(--sage-dark);
		font-weight: 700;
		text-underline-offset: 4px;
	}

	.quote-band {
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		justify-items: start;
		gap: 16px;
		padding-block: clamp(60px, 9vw, 112px);
		background: var(--sage-dark);
		color: var(--white);
	}

	.quote-band .eyebrow {
		color: var(--butter);
	}

	.quote-band h2 {
		margin-bottom: 0;
		font-size: clamp(2rem, 4vw, 3.6rem);
	}

	.quote-title-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 52px;
		padding: 12px 20px;
		border: 1px solid rgba(255, 253, 248, 0.28);
		border-radius: 7px;
		background: rgba(255, 253, 248, 0.08);
		color: var(--white);
		font-size: clamp(1.7rem, 3.5vw, 3rem);
		font-weight: 500;
		line-height: 1;
		text-decoration: none;
		transition:
			transform 180ms ease,
			background-color 180ms ease,
			border-color 180ms ease,
			box-shadow 180ms ease;
	}

	.quote-title-link:hover,
	.quote-title-link:focus-visible {
		transform: translateY(-1px);
		background: rgba(255, 253, 248, 0.14);
		border-color: rgba(255, 253, 248, 0.42);
		box-shadow: 0 12px 28px rgba(17, 28, 26, 0.16);
	}

	.institution-section {
		padding-top: clamp(44px, 6vw, 72px);
		background: var(--sage-dark);
		color: var(--white);
	}

	.institution-section .eyebrow {
		color: var(--butter);
	}

	.institution-section h2,
	.institution-section p,
	.institution-section .inline-link {
		color: var(--white);
	}

	.contact-section {
		align-items: center;
		padding-block: clamp(56px, 8vw, 88px);
		background:
			linear-gradient(120deg, rgba(255, 253, 248, 0.45), transparent 58%),
			var(--butter);
	}

	.contact-section .button.secondary {
		background: transparent;
		border-color: rgba(34, 48, 43, 0.28);
	}

	.site-footer {
		display: flex;
		flex-wrap: wrap;
		justify-content: space-between;
		gap: 12px 24px;
		padding-block: 28px;
		background: var(--ink);
		color: var(--white);
	}

	.site-footer p {
		margin: 0;
	}

	.site-footer a {
		text-decoration-color: rgba(255, 253, 248, 0.5);
		text-underline-offset: 4px;
	}

	@media (prefers-reduced-motion: reduce) {
		*,
		*::before,
		*::after {
			scroll-behavior: auto !important;
			transition-duration: 0.01ms !important;
			animation-duration: 0.01ms !important;
			animation-iteration-count: 1 !important;
		}
	}

	@media (max-width: 900px) {
		.site-header {
			align-items: flex-start;
			flex-direction: column;
			gap: 14px;
		}

		.main-nav {
			width: 100%;
			overflow-x: auto;
			flex-wrap: nowrap;
			justify-content: flex-start;
			padding-bottom: 2px;
			scrollbar-width: none;
		}

		.main-nav::-webkit-scrollbar {
			display: none;
		}

		.hero,
		.institution-section,
		.prose-section,
		.quote-band,
		.contact-section {
			grid-template-columns: 1fr;
		}

		.hero {
			min-height: auto;
		}

		.portrait-panel {
			min-height: 360px;
			aspect-ratio: 16 / 11;
		}

		.portrait-panel img {
			object-position: center 36%;
		}

	}

	@media (max-width: 560px) {
		.site-header,
		.hero,
		.section,
		.quote-band,
		.contact-section,
		.site-footer {
			padding-inline: 18px;
		}

		h1 {
			font-size: 2.5rem;
		}

		h2 {
			font-size: 2rem;
		}

		.hero-lede,
		.section p,
		.contact-section p {
			font-size: 1rem;
		}

		.hero-actions,
		.contact-actions {
			align-items: stretch;
			flex-direction: column;
		}

		.button {
			width: 100%;
		}

	}
</style>
